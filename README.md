# oddsportal scrapper nba

# Crée un nouvel environnement virtuel (optionnel mais recommandé)
python -m venv venv
source venv/Scripts/activate  # ou venv\Scripts\activate.bat sous CMD

# Installe les dépendances
pip install -r requirements.txt

# Installe les navigateurs Playwright
playwright install
